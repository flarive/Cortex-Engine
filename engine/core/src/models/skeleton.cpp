#include "../../include/models/skeleton.h"


//engine::BoneInfo* engine::Skeleton::findBone(const std::string& name) const
//{
//    auto iter = m_boneInfoMap.find(name);
//    if (iter == m_boneInfoMap.end())
//        return nullptr;
//
//    return const_cast<BoneInfo*>(&iter->second);
//}
